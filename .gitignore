# 子项目均为独立 Git 仓库（见 docs/git-repository-management.md）。
# 按引擎层级模型分组（见 docs/project-responsibilities.md §2）。
# 本地开发阶段解耦；待远程仓库就绪后，用 submodule 正式关联。
format/Cartridge/
asset-types/eidolon-character/
runtime/eidolon-runtime/
editor/eidolon-studio/

# Agent 项目记忆（私有数据，不纳入仓库）
.workbuddy/

# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
.pytest_cache/
.coverage
