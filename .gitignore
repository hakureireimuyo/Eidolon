# PersonaSeed 作为独立 Git 仓库管理（见 docs/git-repository-management.md）。
# 本地开发阶段与之解耦；待远程仓库就绪后，用 submodule 正式关联：
#   git submodule add <persona-seed-remote-url> PersonaSeed
PersonaSeed/
eidolon-character/

# Agent 项目记忆（私有数据，不纳入仓库）
.workbuddy/

# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
.pytest_cache/
.coverage
